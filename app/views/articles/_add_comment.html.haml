.center.jumbotron.dark
  %h2.pull-left Add a comment
  = form_for([@article, @article.comments.build]) do |f|
    %p=f.text_area :body, {class: 'grey-text-area form-control', rows: '3'}
    =f.submit
  
